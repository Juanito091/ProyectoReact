import React from 'react';

const CartWidget = () => {
    return (
        <div>
            <i className="bi bi-cart fs-2 text-white"></i>
            <span className="badge bg-secondary ms-2">5</span>
        </div>
    );
};

export default CartWidget;
